{
  "name": "rate-limiter-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "migrate": "node src/db/migrate.js",
    "test": "node test/unit/run-tests.js",
    "test:integration": "node test/integration/concurrency.test.js",
    "test:all": "npm run test && npm run test:integration",
    "load:test": "node scripts/load-test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "supertest": "^7.1.4"
  },
  "engines": {
    "node": "24.x"
  }
}
